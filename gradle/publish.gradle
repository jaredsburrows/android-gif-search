apply {
  plugin("com.github.triplet.play")
}

// Publishing to google play
play {
  serviceAccountEmail = "jaredsburrows@gmail.com"
  pk12File = rootProject.file("config/signing/key.p12")
  track = "production" // or "beta" or "alpha"
}
